import {Dimensions} from 'react-native';

const SIZES = {
  screenHeight: Dimensions.get('window').height,
  screenWidth: Dimensions.get('window').width,
};

export {SIZES};

export const HEADER_PROFILE_HEIGHT = 300;
